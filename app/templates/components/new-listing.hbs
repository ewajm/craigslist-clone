{{#if addNewListing}}
  <h1>New Listing</h1>
  <div>
    <form {{action 'saveListing' on='submit'}}>
      <div class="form-group">
        <label for="title">Title</label>
        {{input value=title id="title" required='required'}}
      </div>
      <div class="form-group">
        <label for="category">Category</label>
        <select required class="form-control" name="category" onchange={{action "updateValue" value="target.value"}}>
          <option value="" selected disabled>Please choose one:</option>
          {{#each categories as |category|}}
            <option value={{category.id}}>{{category.name}}</option>
          {{/each}}
        </select>
      </div>
      <div class="form-group">
        <label for="location">Location</label>
        {{input value=location id="location" required='required'}}
      </div>
      <div class="form-group">
        <label for="price">Price</label>
        {{input value=price id="price" type='number' required='required'}}
      </div>
      <div class="form-group">
        <label for="image">Image URL</label>
        {{input value=image id="image"}}
      </div>
      <div class="form-group">
        <label for="description">Description</label>
        {{input value=description id="description"}}
      </div>
      <button class="btn" >Save</button>
    </form>
  </div>
{{else}}
  <button class="btn" {{action 'listingFormShow'}}>+ New Listing</button>
{{/if}}
